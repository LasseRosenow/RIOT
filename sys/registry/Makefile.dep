ifneq (,$(filter registry_storage%,$(USEMODULE)))
  include $(RIOTBASE)/sys/registry/storage/Makefile.dep
endif

ifneq (,$(filter registry_namespace%,$(USEMODULE)))
  include $(RIOTBASE)/sys/registry/namespace/Makefile.dep
endif

ifneq (,$(filter registry_%,$(USEMODULE)))
  USEMODULE += registry
endif

USEMODULE += fmt
USEMODULE += base64