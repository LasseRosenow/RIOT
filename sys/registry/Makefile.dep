ifneq (,$(filter registry_storage_facility_%,$(USEMODULE)))
  include $(RIOTBASE)/sys/registry/storage_facility/Makefile.dep
endif

ifneq (,$(filter registry_schema_%,$(USEMODULE)))
  include $(RIOTBASE)/sys/registry/schema/Makefile.dep
endif

ifneq (,$(filter registry_%,$(USEMODULE)))
  USEMODULE += registry
endif
