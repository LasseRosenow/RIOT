ifneq (,$(filter registry_cli%,$(USEMODULE)))
  USEMODULE += registry_path
endif

ifneq (,$(filter registry_storage%,$(USEMODULE)))
  include $(RIOTBASE)/sys/registry/storage/Makefile.dep
endif

ifneq (,$(filter registry_schema%,$(USEMODULE)))
  include $(RIOTBASE)/sys/registry/schema/Makefile.dep
endif

ifneq (,$(filter registry_%,$(USEMODULE)))
  USEMODULE += registry
endif
